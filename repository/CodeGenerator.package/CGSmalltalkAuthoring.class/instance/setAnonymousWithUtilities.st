code-authoring
setAnonymousWithUtilities
	" Private - This is suitable for Squeak "

	| authorClass |
	
	authorClass := Smalltalk at: #Utilities. 
	identity := (authorClass canUnderstand: #authorInitialsPerSe)
				ifTrue: [ authorClass authorInitialsPerSe ]
				ifFalse: [ self notify: 'Cannot guess selector for getting author name. Setting empty String temporarily (do not forget to set back initials after finishing generating code'.
						String empty ].
	authorClass setAuthorInitials: self autogeneratedName.


