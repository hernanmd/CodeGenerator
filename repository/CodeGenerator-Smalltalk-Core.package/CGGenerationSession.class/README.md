I own everything that is per-run in a code-generation session: the current target class, the transformer port used for all image mutation, the resolver and name policy, and the set of selectors I generated (for clean-by-identity). I am constructed fresh per run from a CGGenerationContext, so sequential or interleaved runs never share target class, prefix, or clean state (isolation). Concrete generators compose me rather than subclassing the engine.